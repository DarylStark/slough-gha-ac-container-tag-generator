---
name: test action
on:
  push:

jobs:
  test-action:
    runs-on: ubuntu-latest
    outputs:
      tags: ${{ steps.generate-version.outputs.tags }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        id: generate-version
        with:
          container-repository: test-repo
  publish:
    runs-on: ubuntu-latest
    needs: test-action
    steps:
      - run: echo ${{ needs.test-action.outputs.tags }}
