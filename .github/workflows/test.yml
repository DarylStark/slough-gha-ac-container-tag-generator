---
name: test action
on:
  push:

jobs:
  test-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./
        with:
          version: 1.0.0
          container-repository: test-repo
          container-name: test-name
